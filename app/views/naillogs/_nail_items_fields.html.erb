<div class="log-nail-item flex items-center gap-2 mb-1" data-log-nails-target="row">
  <%= f.select :nail_item_id,
        options_from_collection_for_select(
          nail_items,
          :id,
          ->(item) { "#{item.brand.name} / #{item.product.name} / #{item.prod_color.name}" },
          f.object.nail_item_id
        ),
        { include_blank: "--" },
        class: "select w-50 text-[0.6rem] leading-[0.85rem]" %>

  <% if f.object.persisted? %>
    <%= f.check_box :_destroy %>
    <%= f.label :_destroy, "削除", class: "text-xs" %>
  <% else %>
    <button type="button"
            data-action="log-nails#remove"
            class="btn btn-outline text-xs">
      取消
    </button>
  <% end %>
</div>
