<% if page.current? %>
  <span class="bg-primary text-white px-2 py-0 rounded"><%= page %></span>
<% else %>
  <%= link_to page, url, remote: remote, rel: page.rel, class: "px-2 text-gray-800 font-light" %>
<% end %>