<% @brands.each do |brand| %>
	<li class="w-full flex items-center px-2 py-0 hover:bg-gray-100"
			role="option"
			data-autocomplete-label="<%= brand.id %>"
			data-autocomplete-value="<%= brand.name %>">
		<%= brand.name %>
	</li>
<% end %>